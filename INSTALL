1) bundle install
	Depending on your OS, good luck with nokogiri and execjs!
2) Edit the conf/database.yml to point at a valid mysql db for both test and development
3) rake db:create:all
4) rake db:migrate
3) test with
	rake rspec
4) start with, natch
	rails s
